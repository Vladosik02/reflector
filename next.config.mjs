/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Здесь позже добавятся настройки изображений (allowed remote patterns),
  // ENV для бэкенд-сервиса распознавания и т.п.
};

export default nextConfig;
